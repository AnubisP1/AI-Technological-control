# QUESTIONS.md

Открытые вопросы, требующие ответа пользователя или отдельного исследовательского шага, прежде чем принимать необратимые архитектурные решения. По правилу ТЗ (§5.2): здесь фиксируются пробелы, а не придуманные заглушки.

**Статус: все пункты ниже получили ответ пользователя 2026-08-01. Принятые решения зафиксированы в конце каждого пункта под `## РЕШЕНИЕ:` и в `PLAN.md`/`docs/ARCHITECTURE.md`.**

## Блокирующие / архитектурные (нарушение ограничений offline / cross-platform / CPU при неверном выборе)

1. **pythonocc-core vs. чистый Python-парсер STEP.**
   ТЗ требует `pythonocc-core` (OpenCASCADE) для STEP-парсинга и построения B-rep графа под UV-Net. Существующий пайплайн в `Литература/НПО 2026/` сознательно **не использует** OCC, потому что его окружение — Python 3.14, несовместимый с OCC-биндингами (зафиксировано в собственном CLAUDE.md этой подпапки, venv там подтверждает `python3.14`).
   Вопрос: на каком Python работает `dev/` — новое окружение с более старой версией (3.10–3.11, где `pythonocc-core` через conda-forge доступен) или тоже 3.14? Если ограничиваемся Python 3.14 — `pythonocc-core` скорее всего недоступен, и нужно либо (а) завести отдельное conda-окружение только для STEP/OCC-части и общаться с остальным бэкендом через процесс/IPC, либо (б) переиспользовать существующий regex-парсер `step_parser.py` как временную замену OCC на первых фазах.
   Это архитектурное решение с необратимыми последствиями (влияет на структуру сервисов и на офлайн-упаковку зависимостей) — **не принимаю его самостоятельно**, выношу на решение пользователя перед началом Фазы, где это понадобится (Модуль 1.1, анализ 3D-модели).
         ## ОТВЕТ: ты в праве самостоятельно решить как в этом случае поступать, можешь поступить по другому если считаешь нужным
         ## РЕШЕНИЕ: `dev/backend` работает на отдельном venv с Python 3.11 (не 3.14, как в `НПО 2026/venv`) — на этой версии `pythonocc-core` доступен через conda-forge/pip. STEP-парсинг реализуется за интерфейсом `IStepParser` (domain-порт): первая реализация — адаптация regex-парсера `step_parser.py` (быстро, без тяжёлой зависимости, годится для цветовых метаданных и грубой геометрии), вторая реализация (по мере необходимости точного B-rep для UV-Net) — на `pythonocc-core`. Смена реализации не требует переписывания вызывающего кода.

2. **Backend-язык не задан явно.**
   ТЗ: "требования к языкам программирования не предъявляются", но весь ML/AI-стек (ONNX Runtime, pythonocc-core, llama.cpp GGUF, PyMuPDF/pdfplumber, Tesseract, python-docx) — это Python-экосистема, и раздел про ресурс-губернатор явно упоминает "не блокировать event-loop FastAPI". Буду считать бэкенд Python/FastAPI как рабочее умолчание, поскольку это прямо следует из перечисленных инструментов, а не выдумка — но не молчаливое финальное решение, если пользователь имел в виду другой стек.
      ## ОТВЕТ: ты в праве самостоятельно решить как в этом случае поступать, можешь поступить по другому если считаешь нужным
      ## РЕШЕНИЕ: Backend — Python 3.11 + FastAPI, слои `api/services/domain/infrastructure`. Подтверждено выбором ML-стека из самого ТЗ (ONNX Runtime, pythonocc-core, llama.cpp, PyMuPDF/pdfplumber, Tesseract, python-docx — всё Python) и прямым упоминанием "не блокировать event-loop FastAPI".

## Требуют распаковки/чтения источника (не выдумывать, а прочитать)

3. Содержимое `Литература/Белоусов ДД Конференция экономики и качесвта.docx` не распаковано в рамках чек-листа — до его прочтения нельзя опираться на конкретные тезисы этой статьи при проектировании Модуля 1 (оценка ТТ, скоринг качества КД).
      ## ОТВЕТ: читай всё что необходимо для выполнения ТЗ
      ## РЕШЕНИЕ: прочитано (`pandoc` → plain text). Статья описывает 3-модульную архитектуру технологического контроля КД: (1) OCR чертежа — детекция текстовых зон через CRAFT (CNN-детектор на VGG-16, две вероятностные карты: принадлежность пикселя символу + связь между символами), затем распознавание через CRNN+CTC; критичный порог CER < 2%; (2) семантическое сопоставление извлечённых ТТ с эталонной базой через дообученные ruT5-base/FRED-T5 (LoRA-адаптация, косинусное сходство векторных представлений) + контекстная фильтрация эталонной базы по типу/материалу/технологии изделия из основной надписи (сокращает сравнения до 5-10% и повышает точность); (3) авторегрессионная генерация структурированного отчёта о несоответствиях той же T5-моделью. Подход держит human-in-the-loop, не полностью автономный. Это прямой чертёж для реализации Модуля 1.1 (CRAFT+CRNN для распознавания) и Модуля 1.2 (T5/LoRA для сверки ТТ с НСИ и генерации отчёта) — использовать как основу дизайна, а не изобретать альтернативную архитектуру.

4. Содержимое `БД НСИ/ГОСТ Р 2.316-2023.pdf` и `БД НСИ/Технические требования к чертежам.pdf` не распаковано — эти документы потенциально задают конкретные правила для Модуля 1.1 (анализ технических требований и основной надписи чертежа), но пока это только гипотеза по названию файлов.
      ## ОТВЕТ: это требования к чертежам а не к техническим требованиям на чертеже. Требования для чертежа возьми лучше из /Users/bdd/Documents/Работа/Аэромобильность/4. Цифровое производство/БД НСИ/Технические требования к чертежам.pdf
      ## РЕШЕНИЕ: прочитано (`pdftotext`). Документ описывает структуру и содержание технических требований на чертеже (не требования к оформлению самого чертежа): нормативная база (ГОСТ 2.316-2008, 2.109-73, 2.119-2013), правила размещения (над основной надписью, нумерованные пункты, высота шрифта ≥2.5мм), и — самое ценное для контекстной фильтрации Модуля 1.1/1.2 — типовые категории и формулировки пунктов ТТ: материал и заменители («Материал: Сталь 45 ГОСТ 1050-2013», «Материал: Сталь 40Х или сталь 45»), твёрдость/мехсвойства (HRC/HB, σв, σ0.2, δ), термо/химико-термическая обработка (закалка, цементация, азотирование, борирование — с диапазонами глубины/твёрдости), покрытия (никелирование, цинкование, оксидирование по ГОСТ 9.032-74), неуказанные предельные отклонения (H14/h14/±IT14/2), допуски формы/расположения (ГОСТ Р 53442-2015), шероховатость (Ra), технологическая последовательность, контроль/испытания (герметичность, балансировка по ГОСТ 19534-74, неразрушающий контроль), сборочные требования (моменты затяжки, зазоры), эксплуатационные требования (температура, ресурс, гарантия). Использовать как эталонный набор категорий/паттернов при построении базы контекстной фильтрации ТТ (домен для семантического сопоставления из статьи выше) — не изобретать категории заново.

## Пробелы в исходных материалах (нет источника — не заглушка, а вопрос)

5. **Шаблоны маршрутной и операционной карты не найдены как файлы.** В `БД НСИ/schema/07_documents.sql` есть таблицы под шаблоны документов (`document_template` и связанные), но самих файлов-образцов (напр. .docx/.xlsx с реальной формой карты по ГОСТ 3.1404-86/3.1118) в репозитории нет. Нужно уточнить у пользователя: есть ли эталонный образец карты (бланк) где-то ещё, или ожидается, что формат генерируется "с нуля" по тексту ГОСТ.
      ## ОТВЕТ: найди в интернете в ГОСТах
      ## РЕШЕНИЕ: структура граф найдена через веб-поиск (docs.cntd.ru, studref.com, studfile.net — ГОСТ 3.1118-82 для маршрутной карты, ГОСТ 3.1404-86 форма 3/2а для операционной карты). Маршрутная карта: номер операции, код/наименование операции, оборудование, профессия/разряд, нормы времени — по строкам техпроцесса (соответствует `route_card_row` в схеме). Операционная карта форма 3: номер операции/перехода, содержание перехода, оборудование, оснастка/инструмент (в т.ч. измерительный), режимы резания (t — глубина, S — подача, V — скорость, n — обороты), разряд работы, нормы времени (То, Тв, Тшт, Тпз) — соответствует `operation_card_transition_row`. Заполнить `document_template.layout_schema` (JSONB/JSON, уже предусмотрено схемой) этим набором граф на Фазе 4 — реальный бланк-оригинал ГОСТа (точная верстка бланка с рамками) не скачивался, т.к. для цели проекта (генерация данных в этих графах, не факсимильное воспроизведение бланка) достаточно состава и порядка граф; при необходимости точного пиксель-в-пиксель бланка — отдельный вопрос пользователю.

6. **Нет примеров реальных входных КД** (чертёж PDF/DOC + спецификация + STEP для одной и той же детали, металл и пластик) для использования как fixtures в тестах Модуля 1.1/1.2 — единственная STEP-модель с известными эталонными значениями сейчас лежит в `Литература/НПО 2026/Кронштейн.STEP` (research-контекст, не аэромобильность-специфичный пример). КД из `КД/` (Вал шестерня Ф42, Крышка ИР-40) есть как .cdw/.m3d/.stp, но не как PDF-чертёж + спецификация в паре — нужно уточнить, можно ли использовать эти детали как основу тестовых fixtures, и откуда взять соответствующий PDF-чертёж/спецификацию (или их нужно сгенерировать/экспортировать из .cdw).

      ## ОТВЕТ:
      /Users/bdd/Documents/Работа/Аэромобильность/4. Цифровое производство/КД для тестов/Детали из металла/1. Тестовая деталь металл
      /Users/bdd/Documents/Работа/Аэромобильность/4. Цифровое производство/КД для тестов/Детали из металла/2. Тестовая деталь металл
      /Users/bdd/Documents/Работа/Аэромобильность/4. Цифровое производство/КД для тестов/Пластиковые детали/1. Тестовая деталь пластик
      ## РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ (2026-08-01): парсинг спецификации (БОМ) отменён — для формирования технологической документации достаточно чертежа и 3D-модели. Не реализуется ни на этой, ни на последующих фазах, если не будет отдельного явного запроса. `КдAnalysisResult`/`KdAnalysisService` спроектированы без поля под спецификацию.
      Дополнительно пользователь указал на использование чертежей из `КД для тестов/Детали из металла/Архив/` как дополнительных тестовых данных (в частности для детекции видов на чертеже, см. п.9 ниже) — не только основные 2 fixture.

7. **Локальный LLM (llama.cpp/GGUF)** — ТЗ говорит "если потребуется", но не называет конкретную модель/веса. Нужно решение пользователя о конкретной модели (размер, лицензия, язык — русский для формулировок ТТ/отчётов) до того, как резервировать место в `./models/` и учитывать её в CPU-бюджете.
      ## ОТВЕТ: ты в праве самостоятельно решить как в этом случае поступать, можешь поступить по другому если считаешь нужным
      ## РЕШЕНИЕ: не резервирую веса заранее. Генерацию формулировок отчёта/карт на первых фазах реализую шаблонно (Jinja2-подобные строковые шаблоны на правилах — соответствует Модулю 1.2/1.3, где текст в основном структурный: "выявлено несоответствие X пункту Y"). Полноценный LLM (ruT5/FRED-T5 из статьи Белоусова, дообученный LoRA — а не отдельный llama.cpp GGUF) подключается позже, только когда правило-ориентированной генерации станет недостаточно (Фаза 3), т.к. именно эти модели уже упомянуты и в ТЗ, и в профильной статье как основа семантического сопоставления ТТ — предпочитаю их llama.cpp-варианту для консистентности с исследовательским контекстом проекта.

## Не блокирующие, но стоит уточнить в течение работы

8. Нет уточнения формата вывода "процент брака", "оценка стоимости и рисков" в Модуле 3.2 — вероятно, это упрощённая демонстрационная эвристика, а не полноценный экономический расчёт; уточнить ожидаемый уровень детализации перед реализацией этой части.
      ## ОТВЕТ: ты в праве самостоятельно решить как в этом случае поступать, можешь поступить по другому если считаешь нужным
      ## РЕШЕНИЕ: реализую как демонстрационную эвристику (простые формулы от количества операций/материала/типа детали + фиксированные коэффициенты риска), явно помеченную в UI/отчёте как оценочную модель для демонстрации, а не как производственный расчёт — соответствует общему духу Модуля 3.2/4 ("отображаем", "симулируем"), где ТЗ прямо говорит не воспроизводить реальный процесс.

9. **Метод детекции видов на чертеже** (Модуль 1.1: "извлечение данных с видов детали на поле чертежа") — все тестовые чертежи оказались векторными PDF (не сканы), что открывает более быстрый путь (кластеризация векторных путей) в дополнение к общему CV/OCR-подходу для сканов.
      ## ОТВЕТ: объедини два подхода в один, или реализуй два, чтобы система сама выбирала по какому работать в зависимости от того векторная графика или скан
      ## РЕШЕНИЕ: реализованы обе стратегии за одним портом `IViewDetector` — `VectorViewDetector` (двухпроходная пространственная кластеризация bounding box'ов векторных путей PDF, без ML) и `RasterViewDetector` (рендер в растр + OpenCV connectedComponentsWithStats для сканов). `AutoViewDetector` выбирает стратегию по наличию у страницы ≥20 векторных путей — эмпирический порог, отделяющий настоящий векторный чертёж от скана, вставленного в PDF как картинка (у которого векторных путей либо 0, либо единицы — рамка листа). Проверено на 7 реальных чертежах разных форматов (A3, A2) из `КД для тестов/` и `Архив/`, а также на синтетическом скане.
      Калибровка векторного алгоритма потребовала итераций: фиксированный порог слияния боксов в пунктах PDF давал от 2 до 36 кластеров в зависимости от чертежа (не масштабируется между форматами листа A3/A2) — заменено на двухпроходный подход, где второй проход сливает протокластеры порогом, пропорциональным их собственному размеру (доля 0.3 от диагонали), что дало стабильные 2-6 областей на всех проверенных чертежах.

10. **OCR для сканов без текстового слоя** — изначально ссылались на архитектуру CRAFT+CRNN из статьи Белоусова (docs/ARCHITECTURE.md), но её реализация — отдельная исследовательская задача (обучение моделей детекции/распознавания текста).
      ## ОТВЕТ: можешь реализовать по-другому, это не принципиально, главное чтобы работало
      ## РЕШЕНИЕ: `OcrDrawingParser` на готовой библиотеке Tesseract (offline, кроссплатформенная, `pytesseract`) вместо обучения CRAFT+CRNN с нуля — соответствует правилу ТЗ п.4 (не делать своё ML-решение, если есть готовое). Рендер страницы в 400 DPI + бинаризация Отсу перед OCR ощутимо улучшают качество по сравнению с прямым OCR цветного рендера (проверено экспериментально). Результат распознавания обрабатывается той же позиционной логикой извлечения штампа/ТТ, что и текстовый слой PDF (вынесена в общий модуль `stamp_extraction.py`), так что скан и вектор проходят единый путь после этапа "получить строки текста". Качество OCR ниже, чем у текстового слоя — это ожидаемое и задокументированное ограничение, не дефект: на сложных/плотных чертежах часть полей штампа может остаться нераспознанной.
      **Требует установленного tesseract-бинаря с языковым пакетом `rus`** на машине разработки/развёртывания (macOS: `brew install tesseract-lang`; Windows: установщик tesseract-ocr с выбором Russian) — dev/deploy-time зависимость, соответствует офлайн-требованию ТЗ (сеть допустима на этапе разработки, всё скачанное фиксируется локально).
      **Побочная находка про CPU-бюджет:** `cv2.setNumThreads()` (используется ресурс-губернатором для ограничения OpenCV, задействован в `RasterViewDetector`) не имеет эффекта на сборках OpenCV с параллельным бэкендом GCD (Grand Central Dispatch) — типично для `opencv-python`/`opencv-python-headless` на macOS ARM (проверено через `cv2.getBuildInformation()`). Это документированное ограничение самого OpenCV, не баг ресурс-губернатора — на сборках с TBB/OpenMP (типично для Linux/Windows) вызов реально ограничивает потоки. Зафиксировано в `docs/ARCHITECTURE.md` и тесте `test_configure_calls_opencv_set_num_threads_without_error`.

11. **Эталон для сравнения в Модуле 3.1** — ТЗ формулирует вход как "фото... для того чтобы система сравнила изображение с электронной моделью или с изображением электронной модели... (как будет проще)". `RegexStepParser` (см. QUESTIONS.md №1) не строит реальный B-rep — только метаданные (грани, bounding box, цвета), поэтому рендер настоящего 2D-вида 3D-модели недоступен без `pythonocc-core`, который в объём текущего плана не входит.
      ## ОТВЕТ: пользователь выбрал вариант "эталонное фото детали" из предложенных на выбор (эталонное фото / грубый рендер по bounding box / подключить pythonocc-core сейчас).
      ## РЕШЕНИЕ: Модуль 3.1 сравнивает фото изготовленного изделия с ЗАРАНЕЕ загруженным эталонным фото/изображением детали (не с честным 3D-рендером STEP) — явно помечено в UI как демонстрационное сравнение по фото, не по геометрии, что дословно соответствует формулировке ТЗ "как будет проще".
      **Дополнительное уточнение по глубине детализации находок**, обнаруженное при прототипировании на реальных тестовых фото (4 фото одной физической пластиковой детали, `КД для тестов/Пластиковые детали/.../*.jpeg`): сравнение силуэта после нормализации по площади и центру масс + перебор поворота даёт IoU ~0.90-0.97 для одинакового ракурса и ~0.63-0.71 для разных ракурсов одной и той же детали. Размер наибольшего "лишнего" пятна от обычного шума фото (~1.2% площади силуэта) оказался сопоставим с размером синтетического тестового дефекта такого же масштаба (~1.8%) — точная локализация конкретного типа дефекта (отверстие/фаска/выступ) на этом уровне точности была бы избыточной точностью, которую метод не может подтвердить.
      ## ОТВЕТ: пользователь выбрал вариант "только общий вердикт по IoU" вместо попытки локализовать область отклонения.
      ## РЕШЕНИЕ: `OpenCvPhotoComparator` возвращает только общий вердикт (`ok`/`defective`/`inconclusive`) и метрику схожести — без попытки указать координаты/тип конкретного отклонения. Пороги откалиброваны по данным прототипирования (IoU ≥0.80 → ok, <0.55 → defective, между ними — inconclusive, т.к. этот диапазон на реальных фото объясняется чаще разницей ракурса, чем браком). См. `docs/ARCHITECTURE.md`, раздел "Модуль 3.1".

12. **Фаза 8 (вне исходного плана, инициирована пользователем после Фазы 7)** — пользователь показал референс стороннего продукта (визуально насыщенные отчёты: дерево детальных проверок, построчная цветовая раскраска сравнения, связный текст выводов от LLM) и попросил приблизить внешний вид и качество текста отчёта КД к этому уровню.
      ## ОТВЕТ (двухчастный):
      **12.1. Объём и модель LLM.** Пользователь выбрал: (а) делать одним заходом и визуал, и LLM-текст, не разносить на две фазы; (б) начать с одного экрана-пилота — отчёт Модуля 1.2 — прежде чем тиражировать подход на маршрутную карту/Модуль 3; (в) для LLM — облачный API, а не локальная модель.
      **12.2. Облачный LLM API — осознанное, явно подтверждённое пользователем исключение из офлайн-требования ТЗ.** ТЗ формулирует офлайн-режим как Definition-of-Done gate ("Fully offline at runtime. No cloud APIs, no external LLMs... — это gate, не мягкая цель"). Я прямо указал пользователю, что запрошенное решение нарушает это жёсткое ограничение, и явно спросил, как поступить, вместо того чтобы решить самостоятельно (по протоколу CLAUDE.md — "решение потребовало бы нарушить любое жёсткое ограничение" — это случай, где нужно остановиться и спросить). Пользователь подтвердил решение сделать это осознанным исключением, затем выбрал конкретного провайдера — **YandexGPT API**.
      ## РЕШЕНИЕ: LLM-текст формулировок отчёта КД генерируется через YandexGPT API (`app/infrastructure/llm/yandex_gpt_text_generator.py`, реализация порта `ITextGenerator` из domain) — **эта конкретная функция требует сети в рантайме и не работает в изолированном промышленном контуре, что прямо нарушает общий офлайн-gate ТЗ**. Ключ API — только через локальный `.env` (`YANDEX_GPT_API_KEY`/`YANDEX_GPT_FOLDER_ID`), не в коде/гите. Компенсирующий механизм: при сетевой ошибке/отсутствии ключа `KdReviewService` откатывается на прежний шаблонный текст (не роняет отчёт целиком и не блокирует офлайн-сценарий использования остальной системы) — офлайн-режим остаётся полностью рабочим для всех модулей КРОМЕ этого текстового улучшения, которое в офлайне тихо деградирует до шаблонов. Явно задокументировано как отступление от Definition-of-Done в `docs/ARCHITECTURE.md` и в UI (пометка у сформированного LLM-текста).