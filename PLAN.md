# PLAN.md — план разработки

Основано на `../Техническое задание.md`. Одна фаза = одна ветка `feature/phaseN-...`, вливается в `main` после Definition of Done (см. ТЗ §5.4). Порядок фаз идёт от фундамента (данные, парсинг) к пользовательскому сценарию (Модуль 1 → 2 → 3 → 4), поскольку каждый следующий модуль в бизнес-логике зависит от выхода предыдущего.

Часть решений в этом плане зависит от открытых вопросов в `QUESTIONS.md` (особенно №1 — pythonocc-core) — там, где это критично, фаза помечена как заблокированная до ответа. **Статус на 2026-08-01: все вопросы получили ответ/решение, блокировок нет.**

## Фаза 0 — Каркас проекта и ресурс-губернатор ✅ завершена (см. PROGRESS.md)

- Инициализировать структуру `dev/`: `backend/` (слои `api/services/domain/infrastructure`), `frontend/` (Vite + React 19.2), `models/` (веса, .gitignore), `docs/`.
- Настроить `pyproject.toml`/`requirements.txt` для backend, `package.json` для frontend (React 19.2, Vite 7.3, `@vitejs/plugin-react`, без TypeScript).
- Реализовать единый ресурс-губернатор (модуль в `infrastructure/`): выставляет `OMP_NUM_THREADS`, `torch.set_num_threads`, ONNX Runtime `intra_op_num_threads`/`inter_op_num_threads` по умолчанию `floor(0.4 * cpu_count)`, конфигурируемо через `.env`.
- Каркас FastAPI-приложения с воркер-пулом с ограниченным параллелизмом и очередью для тяжёлых задач (не блокировать event-loop).
- Базовый CI-скрипт локально: `pytest` (backend, пустой smoke-тест), `vite build` (frontend, пустой smoke-компонент).
- DoD: приложение поднимается на путях без хардкода `/`/`\` (через `pathlib`), CPU-губернатор покрыт тестом, который проверяет реальное количество потоков.

## Фаза 1 — БД НСИ: перенос схем в SQLite ✅ завершена (см. PROGRESS.md)

- Адаптировать SQL-схемы `../БД НСИ/schema/*.sql` и `../БД НСИ Аддитив/schema/*.sql` из PostgreSQL-диалекта (`SERIAL`, `JSONB`) в SQLite-совместимый (`AUTOINCREMENT`, `TEXT`/`JSON`), сохранив порядок FK-зависимостей между файлами.
- Прогнать адаптированные схемы + seed-данные на реальном локальном SQLite-файле (в отличие от оригиналов, которые не были прогнаны на живом сервере) — зафиксировать и исправить обнаруженные ошибки.
- Слой `infrastructure/db`: доступ к обеим базам (металл / аддитив) через единый интерфейс с явным выбором схемы по признаку "материал детали" (Металл/Пластик) из Модуля 1.
- DoD: обе схемы создаются и заполняются seed-данными на чистой SQLite без ошибок; тест на выборку из `equipment_type_tooling_type`/`printer_type_material_group` (ключевые таблицы совместимости) проходит.

## Фаза 2 — Модуль 1.1: парсинг входной КД ✅ завершена (см. PROGRESS.md)

3D-часть использует интерфейс `IStepParser` (см. QUESTIONS.md №1) — не заблокирована, решение уже принято (regex-парсер как baseline). PDF/DOC-часть не зависит от этого и может идти параллельно.

- ✅ STEP-парсинг геометрии + цветовых метаданных — `RegexStepParser`, проверен на реальных fixture (в т.ч. с STEP-escape кириллицей от C3D/АСКОН) и на эталонной модели с известной цветовой схемой.
- ✅ PDF-парсинг чертежа (основная надпись, технические требования) — `PdfDrawingParser` на PyMuPDF, проверен на двух реально различных layout-ах штампа.
- ✅ Детекция видов на чертеже — `VectorViewDetector` (кластеризация векторных путей, без ML) + `RasterViewDetector` (CV-сегментация для сканов) за единым портом `IViewDetector`, автовыбор через `AutoViewDetector`. Проверено на 7 реальных чертежах (см. QUESTIONS.md №9).
- ✅ OCR для сканов без текстового слоя — `OcrDrawingParser` на Tesseract (`pytesseract`), не CRAFT+CRNN (решение пользователя, см. QUESTIONS.md №10: не принципиально, какая архитектура, главное чтобы работало). Автовыбор PDF-текст/OCR через `AutoDrawingParser`.
- ✅ Оркестрация через `KdAnalysisService` + `/kd/analyze` эндпоинт (использует Auto*-реализации), тяжёлые операции идут через `worker_pool.run_heavy`.
- ❌ Спецификация (БОМ, состав изделия) — отменена решением пользователя (2026-08-01): для формирования ТД достаточно чертежа и 3D-модели. Не входит в объём проекта.
- DoD: на тестовой КД (оба fixture с чертежом + STEP, плюс дополнительные чертежи из `Архив/`) извлекаются структурированные данные (ТТ/надпись из чертежа, виды на поле чертежа, геометрия+метаданные из STEP) в единый JSON.

## Фаза 3 — Модуль 1.2: сверка с НСИ и оценка КД ✅ завершена (см. PROGRESS.md)

- ✅ Сервис сверки материала/заготовки с БД НСИ (`KdReviewService`) — нормализация марки/ГОСТ, три статуса (matched/partial_match/not_found), не выдумывает совпадение при его отсутствии.
- ✅ Классификация технических требований по 12 категориям из структуры `БД НСИ/Технические требования к чертежам.pdf` — правило-ориентированная (regex), без ML, по плану Фазы 3.
- ✅ Генерация отчёта (`KdReviewReport`) с находками трёх уровней важности (blocking/warning/info) — шаблонная генерация текста находок, не LLM (соответствует решению из ARCHITECTURE.md — LLM подключается позже, если правил станет недостаточно).
- ✅ `POST /kd/review` — эндпоинт сверки, лениво инициализирует metal.sqlite при первом обращении.
- DoD: на обоих fixture с чертежом сервис выдаёт отчёт с конкретными находками (точное совпадение материала на fixture 1, частичное — на fixture 2 из-за отсутствия конкретной марки в demo-НСИ; частичное совпадение заготовки по ГОСТ без типоразмера на fixture 1).

## Фаза 4 — Модуль 1.3: генерация технологической документации 🟡 частично завершена (см. PROGRESS.md)

По решению пользователя (2026-08-01) объём фазы ограничен простейшим правило-ориентированным автоподбором (не полноценное технологическое проектирование с расчётом режимов резания).

**Металл:**
- ✅ `ProcessPlanningService` — упрощённый автоподбор станков/операций по таблицам совместимости НСИ (`equipment_type_material_group`, `equipment_type_workpiece_type`, `equipment_type_operation_type`), без расчёта режимов резания и без анализа формы детали (тип заготовки по умолчанию — прокат сортовой).
- ✅ `RouteCardGenerator` — генерация маршрутной карты из результата автоподбора по столбцам `document_template.layout_schema` (не хардкод, переживает правки состава граф в БД).
- ✅ `POST /kd/route-card` — эндпоинт: чертёж → разбор → автоподбор → карта.

**Пластик (аддитивные технологии):**
- ✅ `PrintProcessPlanningService` — автоподбор принтера и цепочки постобработки по правилам additive-НСИ (`printer_type_material_group`, `material_group_pp_tooling_type`). В отличие от металла, технология печати и материал не извлекаются из документа (по ТЗ для пластика на входе только STEP, без чертежа) — указываются явно в запросе.
- ✅ `PrintCardGenerator` — карта техпроцесса печати + карта постобработки по шаблонам `am_document_template` (`PRINT_CARD`/`PP_CARD`).
- ✅ `POST /print/route-card` — эндпоинт: STEP → разбор (для имени детали) → автоподбор по явно переданным технологии/материалу → две карты.

**Общее по обеим веткам:**
- ❌ Операционная карта (детализация по переходам с режимами резания/параметрами печати) — не реализована: требует расчёта режимов резания (t/S/V/n) или параметров печати (высота слоя, время, расход), которого нет в объёме упрощённого автоподбора; графы оставлены пустыми, а не выдуманы. Отложено пользователем на реализацию после завершения основного плана.
- ⬜ Фронтенд-экран загрузки КД и отображения карт — не реализован на этой фазе.
- DoD (частично): на тестовой КД (металл) и тестовом STEP (пластик) сервисы подбирают оборудование/операции по правилам совместимости и генерируют карты с реальными столбцами шаблона; полного прохода с UI и офлайн-прогоном нет.

## Фаза 5 — Модуль 2: согласование и симуляция изготовления ✅ завершена (см. PROGRESS.md)

Поскольку фронтенд-экран Модуля 1 не был реализован на Фазе 4 (см. её DoD), в объём этой фазы пришлось включить и его — без него не из чего было собирать вход для Модуля 2 в реальном UI-сценарии.

- ✅ `Module1.jsx` — экран загрузки КД (выбор металл/пластик, файлы, параметры печати для пластика), отображение результата анализа, отчёта КД, маршрутной карты/карт печати.
- ✅ Согласование комплекта ТД главным технологом (`ApprovalService`, `POST /manufacturing/approval`) — подтвердить (без комментария) / отклонить (комментарий обязателен — иначе технологу нечего исправлять при повторной генерации).
- ✅ Симуляция изготовления (`ManufacturingSimulationService`, `POST /manufacturing/simulate/metal|print`) — строится из уже полученного результата автоподбора техпроцесса (Модуль 1.3), не дублирует подбор оборудования.
- ✅ Универсальные шаблоны управляющих программ (`infrastructure/manufacturing/program_templates.py`) — NC G-code по типу станочной операции (точение/фрезерование/сверление/шлифование; для контроля программы нет — не станочная операция) и печатные программы по технологии (FDM/SLA-MSLA/SLS).
- ✅ `Module2.jsx` — экран согласования + симуляция: прогресс-шкала на 15 секунд (`requestAnimationFrame`, доли по `duration_share`), статичные SVG-иконки станков/принтеров (`MachineIcon.jsx`, без внешних изображений — офлайн-совместимо), таймлайн операций, построчное появление строк программы синхронно с прогрессом текущей операции.
- ✅ Проверено вживую (Vite dev-сервер + backend, Playwright-драйвер, реальные fixture металл/пластик): полный путь "загрузка → анализ → карта → согласование → 15-секундная симуляция с построчным G-code → кнопка анализа качества", включая ветку отклонения с комментарием.
- DoD выполнен: сценарий воспроизводится в UI без реального обращения к оборудованию; офлайн (нет внешних сетевых зависимостей — иконки инлайн-SVG, все данные из локального SQLite).

## Фаза 6 — Модуль 3: оценка качества по фото ✅ завершена (см. PROGRESS.md)

- ✅ Загрузка фото готового изделия; сравнение с эталонным фото (не с 3D-рендером — см. QUESTIONS.md №11, `pythonocc-core`/честный B-rep вне объёма плана) для выявления явных отклонений формы — по ТЗ демонстрационное сравнение по фото, не точная геометрическая проверка. `OpenCvPhotoComparator` — сравнение силуэта (Otsu-сегментация + нормализация по площади/центру масс + перебор поворота + IoU), без ML.
- ✅ Три исхода (`ok`/`defective`/`inconclusive`), откалиброванные на реальных тестовых фото — при недостаточной уверенности (типично при разнице ракурса съёмки) система не выдаёт "брак"/"норма" произвольно, а честно возвращает `inconclusive`.
- ✅ Ветвление: брак → рекомендации по устранению причин (`QualityControlService`); норма → генерация документов для серии (демонстрационная эвристика, см. QUESTIONS.md №8: план/сроки/себестоимость/риск/процент брака от числа операций автоподбора) + рекомендации по оптимизации техпроцесса.
- ✅ Отчёт об оценке качества формируется во всех трёх случаях, включая `inconclusive`.
- ✅ `POST /quality/assess/metal` и `POST /quality/assess/print` — эндпоинты, переиспользуют автоподбор техпроцесса (Модуль 1.3/Фаза 4-5) для построения симуляционного плана, на основе которого считается серийная эвристика.
- ✅ Фронтенд `Module3.jsx` — загрузка эталонного/фактического фото, отображение вердикта, ветвление на карточки "документы для серии" / "рекомендации по браку".
- ✅ Проверено вживую (Playwright, реальные фото готового пластикового изделия + один синтетический тестовый дефект, явно помеченный в коде как синтетический, не выдаваемый за реальное фото брака) — обе ветки (норма/брак) воспроизводятся в браузере.
- DoD выполнен: оба сценария проходимы на реальных/синтетических тестовых фото, отчёт формируется во всех случаях.

## Фаза 7 — Модуль 4: витрина архитектуры серийного производства ✅ завершена (см. PROGRESS.md)

- ✅ `Module4.jsx` — статический/иллюстративный экран архитектуры цеха, переключение металл/пластик (радио-кнопки, без загрузки данных). Пластиковая схема — элементы дословно по перечню ТЗ (термопластавтомат, FDM-конвейерные принтеры CR-30, FDM Bambu Lab, SLS-печать, очистка, обработка порошка, обработка деталей, контроль качества, сборка, энергоблок, склад материалов, офис, склад готовой продукции); металлическая — по аналогии, зоны сгруппированы вокруг типов станков, уже описанных в БД НСИ металла (токарный/фрезерный/сверлильный/шлифовальный участки).
- ✅ Слой IIoT — три условных блока (исполнительные механизмы, датчики контроля, передача телеметрии) под обеими схемами, с пояснением, что это иллюстрация архитектуры, не живая интеграция.
- ✅ Проверено вживую (Playwright, оба режима переключения) — экран рендерится без обращения к backend, переключение мгновенное.
- DoD выполнен: экран отображается, переключение металл/пластик работает без сетевых запросов (иллюстративный модуль, данных для генерации не требует).

## Основной план завершён (Фазы 0-7)

Все фазы, заложенные в исходном плане, реализованы и смержены в `main`. Отложенная работа (операционная карта, режимы резания, параметры печати — см. решение пользователя от 2026-08-01) остаётся на реализацию по отдельному запросу, вне текущего плана.

## Фаза 8 (вне исходного плана, инициирована пользователем) — визуальные улучшения отчёта КД + LLM-резюме ✅ завершена (см. PROGRESS.md)

Инициирована пользователем после Фазы 7 по референсу стороннего продукта (дерево детальных проверок, построчная раскраска, связный текст выводов от LLM). Пилот на одном экране — отчёт Модуля 1.2 (см. dev/QUESTIONS.md №12), прежде чем тиражировать на маршрутную карту/Модуль 3.

- ✅ Дерево проверок в `Module1.jsx` (`ReviewTree`/`TreeNode`) — раскрывающиеся узлы материал/заготовка/технические требования, цветовая раскраска по статусу (matched/partial_match/not_found), построена из уже существующих структурных данных отчёта (не требует LLM).
- ✅ PDF-экспорт отчёта КД (`kd_review_pdf_export.py`, `POST /kd/review/pdf`) — то же дерево, находки, резюме, через PyMuPDF `TextWriter` (обычный `Page.insert_text()` с именованными Base14-шрифтами не поддерживает кириллицу — пришлось перейти на `fitz.Font` + `TextWriter`), с переносом длинных строк по словам.
- ✅ **⚠️ Осознанное, явно подтверждённое пользователем исключение из офлайн-требования ТЗ**: связное LLM-резюме отчёта через облачный YandexGPT API (`YandexGptTextGenerator` за портом `ITextGenerator`). Единственная функция системы, требующая сети в рантайме. При отсутствии ключа или сетевой ошибке `KdReviewService` автоматически откатывается на офлайн `TemplateTextGenerator` — вся остальная система остаётся полностью офлайн.
- ✅ Поле `generated_by: "llm"|"template"` в ответе API и в UI — не выдаём шаблонный текст за LLM-результат и наоборот.
- DoD выполнен: визуальные улучшения и LLM-интеграция проверены вживую (Playwright) на реальном fixture — дерево, PDF-скачивание, шаблонный fallback (реальный вызов YandexGPT не тестировался в CI — юнит-тесты используют mock `ITextGenerator`).

## Фаза 9 (вне исходного плана, инициирована пользователем) — фундамент под переделку фронтенда ✅ завершена (см. PROGRESS.md)

Пользователь запросил полную переделку графического интерфейса под инвесторское демо — бренд "AI-Технолог", БПЛА-домен, AI-ассистент, 3D-вьюер, автоподбор материала пластика, экран цифрового двойника. Детальный план — `/Users/bdd/.claude/plans/abundant-kindling-oasis.md`, фазы 9-16 продолжают нумерацию этого файла.

- ✅ `CLAUDE.md` обновлён под новый стек и бренд/домен, старый стек помечен legacy.
- ✅ `dev/frontend-v2/` — Vite+React+TS+Tailwind/shadcn+Framer Motion+R3F+TanStack Query, порт 5174, dev-proxy на backend.
- ✅ CORS middleware в backend.
- ✅ 3D-вьюер spike (параметрическая модель по реальным размерам STEP), проверен вживую на реальном fixture.
- DoD выполнен: `npm run build` чистый, `pytest` backend 99/99, визуальная проверка через Playwright.

## Фаза 10 — Backend: автоподбор материала для пластика (БПЛА-домен) ✅ завершена (см. PROGRESS.md)

- ✅ Домен `MaterialRecommendationOption`/`MaterialRecommendationResult`, порт `IPrintPlanningLookup` расширен 6 методами (классы применения, условия эксплуатации, рекомендации материала), `MaterialRecommendationService` (откат на базовую рекомендацию класса при отсутствии данных под условия, с явным warning), 3 новых эндпоинта (`GET /print/part-application-classes`, `GET /print/operating-conditions`, `POST /print/material-recommendations`) — читают уже засеянные таблицы `part_application_class`/`operating_condition`/`application_material_recommendation` additive-НСИ. Существующие `/print/route-card` и др. не изменены (проверено diff'ом).

## Фаза 11 — Главная страница + оболочка приложения ✅ завершена (см. PROGRESS.md)

- ✅ Маркетинговый лендинг (hero с параметрическим 3D-вьюером, живой статус backend, секция "как это работает" по 4 модулям, сквозной прогон на реальном fixture вала — цифры сверены с фактическим выводом backend, не выдуманы) + рабочая навигационная оболочка (топбар, сайдбар под БПЛА-модули, live backend-статус) — первая демонстрируемая веха.

## Фаза 12 — Экран "Анализ детали" ✅ завершена (см. PROGRESS.md)

- ✅ Дерево проверки КД (`ReviewTree`, перенесена логика на новый стек), 3D-вьюер интегрирован по-настоящему (реальный `bounding_box` из `/kd/analyze`), блок автоподбора материала (`MaterialRecommendation`, использует Фазу 10, часть эксплуатации выбирается чипами, приоритетная рекомендация — героическая карточка, альтернативы свёрнуты) — без ручных дропдаунов технологии/материала. Проверено end-to-end на обоих реальных fixture (металл-вал и пластик-крышка) через Playwright.

## Фаза 13 — Экраны "Производство" и "Контроль качества"

- ⬜ Перенос логики симуляции (G-code typewriter) и фото-QC на новый стек.

## Фаза 14 — Цифровой двойник

- ⬜ Изометрическая схема цеха + граф-схема IT/IIoT-архитектуры, по образцу `UI UX reference/factory digital twin.html` и `visual factory.html`, симулированная телеметрия на фронте.

## Фаза 15 — AI-ассистент

- ⬜ `POST /assistant/chat` (YandexGPT + keyword-контекст из НСИ, тот же fallback-паттерн, что LLM-резюме отчёта) + фронтенд-панель чата.

## Фаза 16 — Полировка и финальная проверка

- ⬜ Сквозной прогон на реальных fixture (включая детали из `Архив/`), замена `dev/frontend` на `dev/frontend-v2`, обновление `dev/README.md`/`docs/ARCHITECTURE.md`.

## Сквозные задачи (весь проект, не отдельная фаза)

- Поддержание `PROGRESS.md`, `docs/ARCHITECTURE.md`, `docs/INPUTS.md`, корневого `README.md` (офлайн-разворачивание на чистой машине Win/Mac) — обновлять по итогам каждой фазы, а не в конце.
- Нагрузочный тест с измерением CPU (`psutil`) — добавляется/расширяется по мере появления первых ML-инференсов (со Фазы 2), а не откладывается на конец.
- Все секреты/ключи — только через локальный `.env`, не в коде и не в коммитах.
